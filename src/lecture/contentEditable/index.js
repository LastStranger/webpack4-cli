import React from 'react';

const ContentEditable = () => {
    return (
        <p contentEditable>
            this sentence is editable, click here test it out
        </p>
    );
};

export default ContentEditable;
