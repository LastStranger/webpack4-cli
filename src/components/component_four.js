import React from 'react';
import loadsh from "lodash";

const ComponentInside = () => {
    console.log(loadsh);
    return (
        <div>
            this is ComponentInside
        </div>
    );
};

export default ComponentInside;
